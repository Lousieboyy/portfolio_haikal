{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "chrome",
      "request": "launch",
      "name": "Open Portfolio (with server)",
      "url": "http://localhost:8080",
      "webRoot": "${workspaceFolder}",
      "preLaunchTask": "Start Portfolio Server"
    },
    {
      "type": "chrome",
      "request": "launch",
      "name": "Open index.html directly",
      "file": "${workspaceFolder}/index.html"
    }
  ]
}
